# pim-check Docker 환경변수
# cp .env.example .env 후 수정

# 대시보드 포트
DASHBOARD_PORT=8080

# 타겟 설정
TARGET_HOST=192.168.0.5
TARGET_USER=root
TARGET_PASSWORD=root

# 실행 간격 (초)
RUN_INTERVAL=300

# 특정 케이스만 실행 (비우면 태그 기반)
RUN_CASE=

# 태그 필터 (RUN_CASE가 비었을 때 사용)
RUN_TAG=smoke

# 타임존
TZ=Asia/Seoul
