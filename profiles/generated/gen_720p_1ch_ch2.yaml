# Auto-generated by pim-check generator
name: '[auto] 720p_1ch_ch2'
description: 'Auto-generated: 720p 1ch_ch2'
setup:
  edgeconf_changes:
    .VHL_CAM.capture.enable: false
    .VHL_CAM.cam_width: 1280
    .VHL_CAM.cam_height: 720
    .VHL_CAM.i2c2.ch0.enable: false
    .VHL_CAM.i2c2.ch1.enable: false
    .VHL_CAM.i2c1.ch2.enable: true
    .VHL_CAM.i2c1.ch3.enable: false
  reboot_after: true
  stabilize_sec: 30
checks:
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  recording:
    expected_channels: 1
    session_progress: 1/1
  custom_commands:
  - name: i2c-2 AP1302 미응답 (1ch_ch2)
    command: i2cdetect -y 2 2>/dev/null | awk 'NR>1 {for(i=2;i<=NF;i++) if ($i=="11"
      || $i=="12") print $i}' | sort -u | tr '\n' ',' | sed 's/,$//'
    expected: ''
    on_fail: 1ch_ch2인데 i2c-2에 AP1302 응답
  - name: i2c-1 single mode 확인 (ch2 only → 3c)
    command: i2cdetect -y 1 2>/dev/null | awk 'NR>1 {for(i=2;i<=NF;i++) if ($i=="11"
      || $i=="12") print $i}' | sort -u | tr '\n' ',' | sed 's/,$//'
    expected: ''
    on_fail: 1ch_ch2 기대 single인데 0x11 또는 0x12가 응답함
