# Auto-generated by pim-check generator
name: '[auto] 720p_mux_mp4'
description: 'Auto-generated: 720p mux_mp4'
setup:
  edgeconf_changes:
    .VHL_CAM.cam_width: 1280
    .VHL_CAM.cam_height: 720
    .VHL_CAM.muxer: mp4
    .VHL_CAM.i2c2.ch1.enable: false
    .VHL_CAM.i2c1.ch2.enable: false
    .VHL_CAM.i2c1.ch3.enable: false
    .VHL_CAM.i2c2.ch0.enable: true
  reboot_after: true
  stabilize_sec: 30
checks:
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  custom_commands:
  - name: SD 쓰기 가능 확인
    command: test -w /mnt/sd_cam && echo OK || echo FAIL:NO_SD
    expected: OK
    on_fail: SD 미마운트 또는 쓰기 불가
  - name: MP4 파일 생성 확인
    command: sleep 70 && find /mnt/sd_cam -type f -name '*.mp4' -mmin -2 2>/dev/null
      | wc -l | tr -d ' '
    expected_min: 1
    on_fail: MP4 파일 미생성
  - name: TS 파일 없음 확인 (배타)
    command: find /mnt/sd_cam -type f -name '*.ts' -mmin -2 2>/dev/null | wc -l |
      tr -d ' '
    expected: '0'
    on_fail: muxer=mp4 설정인데 TS 파일이 함께 생성됨
