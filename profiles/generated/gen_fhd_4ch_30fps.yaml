# Auto-generated by pim-check generator
name: '[auto] fhd_4ch_30fps'
description: 'Auto-generated: fhd 4ch 30fps'
setup:
  edgeconf_changes:
    .VHL_CAM.capture.enable: false
    .VHL_CAM.cam_width: 1920
    .VHL_CAM.cam_height: 1080
    .VHL_CAM.i2c1.ch2.enable: false
    .VHL_CAM.i2c1.ch3.enable: false
    .VHL_CAM.i2c2.ch1.enable: false
    .VHL_CAM.i2c2.ch0.enable: true
    .VHL_CAM.fps: 30
    .VHL_CAM.recording_time: 1
    .VHL_CAM.muxer: mp4
  reboot_after: true
  stabilize_sec: 60
checks:
  cpu:
    gst_range:
    - 60
    - 100
  thermal:
    warn_temp_c: 88
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  recording:
    expected_channels: 4
  custom_commands:
  - name: edgeconf fps 반영
    command: jq -r '.VHL_CAM.fps' /root/shared_v/edgeconf_pim.json
    expected: '30'
    on_fail: edgeconf fps=30 미반영
  - name: SD 쓰기 가능 확인
    command: test -w /mnt/sd_cam && echo OK || echo FAIL:NO_SD
    expected: OK
    on_fail: SD 미마운트 — fps 실측 검증 전제 조건 불충족
  - name: 실제 스트림 30fps 확인 (ffprobe r_frame_rate)
    command: sleep 75 && LATEST=$(find /mnt/sd_cam -type f \( -name '*.mp4' -o -name
      '*.ts' \) -mmin -2 -printf '%T@ %p\n' 2>/dev/null | sort -n | tail -1 | cut
      -d' ' -f2-); [ -n "$LATEST" ] && FR=$(ffprobe -v error -select_streams v:0 -show_entries
      stream=r_frame_rate -of csv=p=0 "$LATEST" 2>/dev/null) && echo "$FR" | awk -F/
      '{n=$1/$2; if (n>=29.5 && n<=30.5) print "OK"; else printf "FAIL:%.1f\n", n}'
      || echo FAIL:NO_FILE
    expected: OK
    on_fail: 실제 스트림 fps가 30이 아님
