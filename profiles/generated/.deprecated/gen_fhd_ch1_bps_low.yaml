# Auto-generated by pim-check generator
name: '[auto] fhd_bps_low'
description: 'Auto-generated: fhd bps_low'
setup:
  edgeconf_changes:
    .VHL_CAM.cam_width: 1920
    .VHL_CAM.cam_height: 1080
    .VHL_CAM.i2c2.ch0.enable: false
    .VHL_CAM.i2c1.ch2.enable: false
    .VHL_CAM.i2c1.ch3.enable: false
    .VHL_CAM.i2c2.ch1.enable: true
    .VHL_CAM.i2c2.ch1.bps:
    - 1024
    - 512
    .VHL_CAM.recording_time: 1
    .VHL_CAM.muxer: mp4
  reboot_after: true
  stabilize_sec: 30
checks:
  thermal:
    warn_temp_c: 88
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  custom_commands:
  - name: edgeconf bps ch1 반영
    command: jq -c '.VHL_CAM.i2c2.ch1.bps' /root/shared_v/edgeconf_pim.json
    expected: '[1024,512]'
    on_fail: edgeconf bps ch1 미반영
  - name: SD 쓰기 가능
    command: test -w /mnt/sd_cam && echo OK || echo FAIL:NO_SD
    expected: OK
    on_fail: SD 미마운트
  - name: 실측 bitrate ch1 (256~1126 kbps)
    command: sleep 75 && F=$(find /mnt/sd_cam -type f -name '*ch1*.mp4' -mmin -2 -printf
      '%T@ %p\n' 2>/dev/null | sort -n | tail -1 | cut -d' ' -f2-); [ -n "$F" ] &&
      BR=$(ffprobe -v error -select_streams v:0 -show_entries stream=bit_rate -of
      csv=p=0 "$F" 2>/dev/null) && echo "$BR" | awk '{k=$1/1000; if (k>=256 && k<=1126)
      print "OK"; else printf "FAIL:%.0fkbps\n", k}' || echo FAIL:NO_FILE
    expected: OK
    on_fail: ch1 실측 bitrate 범위(256~1126 kbps) 벗어남
