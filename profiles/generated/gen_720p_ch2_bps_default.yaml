# Auto-generated by pim-check generator
name: '[auto] 720p_bps_default'
description: 'Auto-generated: 720p bps_default'
setup:
  edgeconf_changes:
    .VHL_CAM.cam_width: 1280
    .VHL_CAM.cam_height: 720
    .VHL_CAM.i2c2.ch0.enable: false
    .VHL_CAM.i2c2.ch1.enable: false
    .VHL_CAM.i2c1.ch3.enable: false
    .VHL_CAM.i2c1.ch2.enable: true
    .VHL_CAM.i2c1.ch2.bps:
    - 2048
    - 1024
    .VHL_CAM.recording_time: 1
    .VHL_CAM.muxer: mp4
  reboot_after: true
  stabilize_sec: 30
checks:
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  custom_commands:
  - name: edgeconf bps ch2 반영
    command: jq -c '.VHL_CAM.i2c1.ch2.bps' /root/shared_v/edgeconf_pim.json
    expected: '[2048,1024]'
    on_fail: edgeconf bps ch2 미반영
  - name: SD 쓰기 가능
    command: test -w /mnt/sd_cam && echo OK || echo FAIL:NO_SD
    expected: OK
    on_fail: SD 미마운트
  - name: 실측 bitrate ch2 (512~2252 kbps)
    command: sleep 75 && F=$(find /mnt/sd_cam -type f -name '*ch2*.mp4' -mmin -2 -printf
      '%T@ %p\n' 2>/dev/null | sort -n | tail -1 | cut -d' ' -f2-); [ -n "$F" ] &&
      BR=$(ffprobe -v error -select_streams v:0 -show_entries stream=bit_rate -of
      csv=p=0 "$F" 2>/dev/null) && echo "$BR" | awk '{k=$1/1000; if (k>=512 && k<=2252)
      print "OK"; else printf "FAIL:%.0fkbps\n", k}' || echo FAIL:NO_FILE
    expected: OK
    on_fail: ch2 실측 bitrate 범위(512~2252 kbps) 벗어남
