# Auto-generated by pim-check generator
name: '[auto] disk_on_srt_off'
description: 'Auto-generated: disk_on srt_off'
setup:
  ord_vcm_changes:
    .ORD.disk_manage: true
    .ORD.event_auto_remove: true
    .VCM.srt_enable: false
  reboot_after: true
  stabilize_sec: 30
checks:
  cam_state:
    dir: /tmp/cam_state
    expected_state: healthy
    max_streak: 0
  custom_commands:
  - name: config .ORD.disk_manage == true
    command: jq '.ORD.disk_manage' /root/shared_v/ord_vcm_conf.json
    expected: 'true'
    on_fail: .ORD.disk_manage 값이 true와 다름
  - name: config .ORD.event_auto_remove == true
    command: jq '.ORD.event_auto_remove' /root/shared_v/ord_vcm_conf.json
    expected: 'true'
    on_fail: .ORD.event_auto_remove 값이 true와 다름
  - name: config .VCM.srt_enable == false
    command: jq '.VCM.srt_enable' /root/shared_v/ord_vcm_conf.json
    expected: 'false'
    on_fail: .VCM.srt_enable 값이 false와 다름
monitor:
  duration_sec: 0
